# Local runtime mode
NODE_ENV=development

# Telegram bot token from @BotFather
BOT_TOKEN=

# Supabase project URL and service role key (server-side only)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Storage bucket for food diary media (private bucket expected)
SUPABASE_STORAGE_BUCKET=food

# If your users table uses a different telegram id column, change these values
USER_TELEGRAM_ID_COLUMN=telegram_id
USER_ON_CONFLICT_COLUMN=telegram_id

# STT options for future voice support
STT_PROVIDER=none
OPENAI_API_KEY=

# Do not commit real values. In GitHub, add BOT_TOKEN and SUPABASE_* to Actions Secrets.
